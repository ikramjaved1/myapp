<%= form_with(model: physician) do |form| %>
  <% if physician.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(physician.errors.count, "error") %> prohibited this physician from being saved:</h2>

      <ul>
        <% physician.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :dname, style: "display: block" %>
    <%= form.text_field :dname %>
  </div>

  <div>
    <%= form.label :experience, style: "display: block" %>
    <%= form.number_field :experience %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
